/** Recharts 轴/网格 — 跟随 Shell semantic tokens（Studio dark 兼容） */
export const chartTick = { fontSize: 12, fill: 'hsl(var(--muted-foreground))' };
export const chartTickSm = { fontSize: 11, fill: 'hsl(var(--muted-foreground))' };
export const chartTickXs = { fontSize: 10, fill: 'hsl(var(--muted-foreground))' };
export const chartGridStroke = 'hsl(var(--border))';
export const chartPrimaryFill = 'hsl(var(--primary))';

export const chartTooltipStyle = {
  background: 'hsl(var(--card))',
  border: '1px solid hsl(var(--border))',
  borderRadius: '8px',
  color: 'hsl(var(--foreground))',
};
